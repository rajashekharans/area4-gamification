# Continue Here — i-Differentiate Area 4

**Paused:** 2026-06-11 (session limit reached)
**Status:** ✅ PROJECT COMPLETE — all 4 phases built, gated, executed, and browser-verified
**Last commit:** cf741a7 (working tree clean, 30 commits on main)

## What this project is

Single-file HTML gamification leaderboard for Toastmasters Area 4, District 90 (NSW),
replacing `Area4_Gamification_Tracker.xlsx` (source workbook at
`/Users/rajnaidu/Claude/Projects/Area 4 Gamification/`).

## Where everything lives

| Artifact | Path |
|---|---|
| **The app** (deliverable) | `index.html` (84 KB, works from file:// or any static host) |
| Test suite (75/75 green) | `tests.html` — serve over http, e.g. `python3 -m http.server 8642` |
| Sample TMI CSV for testing upload | `sample-data/tmi-club-performance-sample.csv` |
| User guide / hosting steps | `README.md` |
| Design system + architecture (LOCKED) | `docs/superpowers/specs/2026-06-11-area4-gamification-design.md` |
| Phased implementation plan | `docs/superpowers/plans/2026-06-11-area4-gamification-plan.md` |
| Per-phase designs | `docs/superpowers/specs/0{1,2,3,4}-*-phase-design.md` |
| GSD state (all phases ✓) | `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/phases/*/` |
| Preview server config | `/Users/rajnaidu/Projects/.claude/launch.json` (name: `area4`, port 8642) |

## Verified working (2026-06-11, 375px viewport, zero console errors)

Leaderboard seed parity (Quakers Hill 5 / Blacktown City 5 / others 0, Excel tie-rank),
breakdown accordion, snapshot → movement (+31, rank +2 chip), Biggest Mover, trends chart
(brand palette) + history delete, TMI screen, CSV mapping (6/6 clubs, Prospect Phoenix by
name fallback, decoys rejected), minutes extractor ("12 of 18" → 67%), settings (21 weights),
export/import validation, reset.

## Remaining OPTIONAL work (user's call, nothing blocking)

1. **Publish to GitHub Pages** so members get a WhatsApp-clickable link
   (steps already written in README.md; repo has no remote yet — `gh repo create`).
2. **Prospect Phoenix club number** — not in public TMI directory as of 2026-06-11;
   enter in Settings screen when known (stored as null; CSV matches it by name meanwhile).
3. **First real data load** — export Club Performance CSV from
   dashboards.toastmasters.org (District 90), drop into TMI screen, save first snapshot.
4. Possible future ideas (NOT planned): auto-snapshot scheduling, Fireflies meeting-minutes
   integration (user has Fireflies MCP), multi-area support.

## How to resume in a new session

Start Claude Code in `/Users/rajnaidu/Projects/area4-gamification` and say:
> Read .continue-here.md and continue from there.

Useful commands: `/gsd-progress` (state check) · `/gsd-verify-work` (UAT walkthrough) ·
`/gsd-complete-milestone` (archive) · `/gsd-new-milestone` (v2 features).

## Key technical contracts (if modifying the app)

- All raw colors/sizes live ONLY in the `:root` token block of index.html; everything else `var(--token)`.
- Script regions `// === CORE ===` (pure, no DOM, exposed on `window.A4`) and `// === UI ===` —
  tests.html extraction depends on these exact markers.
- State: localStorage key `idiff-area4-v1`; scoring must stay parity with the Excel workbook
  (formulas in design.md §B2); snapshots capped at 60.
- After ANY change: run tests headlessly — extract CORE with node and assert, or open
  http://localhost:8642/tests.html — must stay 75/75 (extend, never delete, cases).
